[project]
name = "app"
version = "0.1.0"
description = "level0 project structure test"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]